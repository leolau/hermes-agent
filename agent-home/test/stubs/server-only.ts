// Test stub for the `server-only` guard. In the app it makes a module a
// server-only module; under vitest (plain Node) it is a harmless no-op so the
// seam modules can be unit/integration tested directly.
export {};
