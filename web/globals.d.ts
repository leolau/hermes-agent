// Ambient declarations for non-code side-effect imports handled by the
// bundler (global stylesheets, third-party CSS). Mirrors the `*.css` shim
// Vite's client types previously provided.
declare module "*.css";
